# Feature Modules

`src/features/` holds domain-owned logic.

Current production-owned feature folders:

- `auth`
- `meals`
- `onboarding`
- `preferences`
- `workouts`

Future folders such as `grocery` and `insights` should only be created when their logic is moved out of route files and into real feature modules.
